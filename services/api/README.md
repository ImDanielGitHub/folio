# Local API service

This package is the Python 3.12 loopback service for the standalone finance agent. The bootstrap commit intentionally contains only package boundaries. The three implementation lanes fill the modules described in the root `BUILD_CONTRACT.md`.
