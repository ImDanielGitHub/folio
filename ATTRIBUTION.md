# Attribution register

## Bootstrap commit

No third-party source code, user-interface assets, prompts, product branding, datasets or minified bundles were copied into the bootstrap.

The repository declares ordinary package dependencies in `services/api/pyproject.toml`. Those packages remain governed by their own licences and are resolved into `services/api/uv.lock`; they are not vendored here.

The `FinanceSurfaceSpec@1` concept and the lifecycle event names are implemented from the behaviour frozen in `BUILD_CONTRACT.md`. No A2UI, AG-UI or other agent-interface runtime or source code is included.

## Required entry format for later adaptations

Before landing adapted third-party code or an asset, add:

| Source | Version/commit | Licence | Files used | Nature of adaptation | Verified by/date |
|---|---|---|---|---|---|
| _No entries yet_ | | | | | |

An inspiration link alone is not a licence. Unclear ownership blocks copying.
