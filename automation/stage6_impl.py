from __future__ import annotations

import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]


def read(path: str) -> str:
    return (ROOT / path).read_text(encoding="utf-8")


def write(path: str, value: str) -> None:
    destination = ROOT / path
    destination.parent.mkdir(parents=True, exist_ok=True)
    destination.write_text(value, encoding="utf-8")


def replace_once(value: str, old: str, new: str, *, label: str) -> str:
    count = value.count(old)
    if count != 1:
        raise RuntimeError(f"{label}: expected one match, found {count}")
    return value.replace(old, new, 1)


def create_runtime_contracts() -> None:
    write(
        "apps/desktop/src/runtimeContracts.ts",
        '''import type {\n  ActivityItem,\n  FinanceAction,\n  FinanceBlock,\n  FinanceSurfaceSpec,\n  RunEvent,\n  SourceItem,\n  ThreadTurn,\n  WorkspaceSnapshot,\n} from "./types.js";\n\ntype JsonRecord = Record<string, unknown>;\n\nconst identifierPattern = /^[a-z][a-z0-9_]{2,112}$/;\nconst hashPattern = /^[a-f0-9]{64}$/;\nconst currencyPattern = /^[A-Z]{3}$/;\nconst surfaceTypes = new Set([\n  "living_brief", "transaction_detail", "cash_scenario",\n  "records_table", "owner_pack", "work_receipt",\n]);\nconst blockTypes = new Set([\n  "narrative", "metric", "cash_series", "scenario_compare",\n  "transaction_rows", "finding", "source_list", "change_diff",\n  "artifact_preview",\n]);\nconst actionTypes = new Set([\n  "focus_source", "open_drawer", "run_scenario", "undo_event",\n  "download_artifact",\n]);\nconst runEventTypes = new Set([\n  "run.started", "message.delta", "message.completed", "stage.started",\n  "stage.completed", "tool.started", "tool.completed", "state.snapshot",\n  "state.patch", "surface.replace", "surface.patch", "receipt.committed",\n  "run.failed", "run.completed",\n]);\n\nfunction record(value: unknown, label: string): JsonRecord {\n  if (value === null || typeof value !== "object" || Array.isArray(value)) {\n    throw new Error(`${label} must be an object.`);\n  }\n  return value as JsonRecord;\n}\n\nfunction exact(\n  value: JsonRecord,\n  allowedKeys: readonly string[],\n  label: string,\n  requiredKeys: readonly string[] = allowedKeys,\n): void {\n  const allowed = new Set(allowedKeys);\n  const unexpected = Object.keys(value).filter((key) => !allowed.has(key));\n  if (unexpected.length) {\n    throw new Error(`${label} contains fields outside the closed contract: ${unexpected.join(", ")}.`);\n  }\n  const missing = requiredKeys.filter((key) => !(key in value));\n  if (missing.length) throw new Error(`${label} is missing required fields: ${missing.join(", ")}.`);\n}\n\nfunction requiredString(value: unknown, label: string, maxLength = 8_000): string {\n  if (typeof value !== "string" || !value.trim() || value.length > maxLength) {\n    throw new Error(`${label} must be non-empty bounded text.`);\n  }\n  return value;\n}\n\nfunction optionalString(value: unknown, label: string, maxLength = 8_000): string | undefined {\n  if (value === undefined) return undefined;\n  return requiredString(value, label, maxLength);\n}\n\nfunction identifier(value: unknown, label: string): string {\n  const result = requiredString(value, label, 113);\n  if (!identifierPattern.test(result)) throw new Error(`${label} is not a valid Folio identifier.`);\n  return result;\n}\n\nfunction safeInteger(value: unknown, label: string): number {\n  if (typeof value !== "number" || !Number.isSafeInteger(value)) {\n    throw new Error(`${label} must be a safe integer.`);\n  }\n  return value;\n}\n\nfunction nonNegativeInteger(value: unknown, label: string): number {\n  const result = safeInteger(value, label);\n  if (result < 0) throw new Error(`${label} must not be negative.`);\n  return result;\n}\n\nfunction finiteNumber(value: unknown, label: string): number {\n  if (typeof value !== "number" || !Number.isFinite(value)) {\n    throw new Error(`${label} must be finite.`);\n  }\n  return value;\n}\n\nfunction booleanValue(value: unknown, label: string): boolean {\n  if (typeof value !== "boolean") throw new Error(`${label} must be boolean.`);\n  return value;\n}\n\nfunction enumValue<T extends string>(value: unknown, allowed: ReadonlySet<T>, label: string): T {\n  if (typeof value !== "string" || !allowed.has(value as T)) {\n    throw new Error(`${label} is outside the closed contract.`);\n  }\n  return value as T;\n}\n\nfunction array(value: unknown, label: string, maxLength: number, minLength = 0): unknown[] {\n  if (!Array.isArray(value) || value.length < minLength || value.length > maxLength) {\n    throw new Error(`${label} violates the closed contract count.`);\n  }\n  return value;\n}\n\nfunction stringArray(value: unknown, label: string, maxLength = 200): string[] {\n  return array(value, label, maxLength).map((item, index) =>\n    requiredString(item, `${label}[${index}]`, 1_000),\n  );\n}\n\nfunction unique(values: readonly string[], label: string): void {\n  if (new Set(values).size !== values.length) throw new Error(`${label} contains duplicate identifiers.`);\n}\n\nfunction dateTime(value: unknown, label: string): string {\n  const result = requiredString(value, label, 80);\n  if (Number.isNaN(Date.parse(result))) throw new Error(`${label} must be an ISO date or date-time.`);\n  return result;\n}\n\nfunction currency(value: unknown, label: string): string {\n  const result = requiredString(value, label, 3);\n  if (!currencyPattern.test(result)) throw new Error(`${label} must be an ISO 4217 code.`);\n  return result;\n}\n\nfunction hash(value: unknown, label: string): string {\n  const result = requiredString(value, label, 64);\n  if (!hashPattern.test(result)) throw new Error(`${label} must be a lowercase SHA-256 digest.`);\n  return result;\n}\n\nfunction evidenceIds(value: unknown, label: string): string[] {\n  const result = stringArray(value, label, 200);\n  result.forEach((item, index) => identifier(item, `${label}[${index}]`));\n  unique(result, label);\n  return result;\n}\n\nfunction freshness(value: unknown, label: string): void {\n  const candidate = record(value, label);\n  exact(candidate, ["dataThrough", "status", "timezone"], label);\n  dateTime(candidate.dataThrough, `${label}.dataThrough`);\n  enumValue(candidate.status, new Set(["current", "stale", "partial"]), `${label}.status`);\n  requiredString(candidate.timezone, `${label}.timezone`, 80);\n}\n\nfunction scalar(value: unknown, label: string): void {\n  if (value === null || typeof value === "string" || typeof value === "boolean") return;\n  if (typeof value === "number" && Number.isFinite(value)) return;\n  throw new Error(`${label} must be a scalar or null.`);\n}\n\nfunction transaction(value: unknown, label: string): void {\n  const row = record(value, label);\n  exact(row, [\n    "transactionId", "occurredOn", "description", "amountMinor", "currency",\n    "classification", "category", "status", "evidenceIds",\n  ], label);\n  identifier(row.transactionId, `${label}.transactionId`);\n  dateTime(row.occurredOn, `${label}.occurredOn`);\n  requiredString(row.description, `${label}.description`, 500);\n  safeInteger(row.amountMinor, `${label}.amountMinor`);\n  currency(row.currency, `${label}.currency`);\n  enumValue(\n    row.classification,\n    new Set(["business", "personal", "unresolved", "transfer"]),\n    `${label}.classification`,\n  );\n  if (row.category !== null) requiredString(row.category, `${label}.category`, 120);\n  enumValue(row.status, new Set(["posted", "pending", "duplicate", "ignored"]), `${label}.status`);\n  evidenceIds(row.evidenceIds, `${label}.evidenceIds`);\n}\n\nfunction scenario(value: unknown, label: string): void {\n  const candidate = record(value, label);\n  exact(candidate, ["label", "lowPointMinor", "reserveShortfallMinor", "currency"], label);\n  requiredString(candidate.label, `${label}.label`, 160);\n  safeInteger(candidate.lowPointMinor, `${label}.lowPointMinor`);\n  nonNegativeInteger(candidate.reserveShortfallMinor, `${label}.reserveShortfallMinor`);\n  currency(candidate.currency, `${label}.currency`);\n}\n\nfunction validateBlock(value: unknown, index: number): FinanceBlock {\n  const label = `surface.blocks[${index}]`;\n  const block = record(value, label);\n  identifier(block.blockId, `${label}.blockId`);\n  const type = enumValue(block.type, blockTypes, `${label}.type`);\n  switch (type) {\n    case "narrative":\n      exact(block, ["blockId", "type", "text", "tone"], label);\n      requiredString(block.text, `${label}.text`, 12_000);\n      enumValue(block.tone, new Set(["neutral", "positive", "caution", "critical"]), `${label}.tone`);\n      break;\n    case "metric":\n      exact(block, ["blockId", "type", "label", "valueMinor", "currency", "evidenceIds"], label);\n      requiredString(block.label, `${label}.label`, 160);\n      safeInteger(block.valueMinor, `${label}.valueMinor`);\n      currency(block.currency, `${label}.currency`);\n      evidenceIds(block.evidenceIds, `${label}.evidenceIds`);\n      break;\n    case "cash_series": {\n      exact(block, ["blockId", "type", "currency", "points", "assumptions", "evidenceIds"], label);\n      currency(block.currency, `${label}.currency`);\n      const points = array(block.points, `${label}.points`, 366, 2);\n      points.forEach((point, pointIndex) => {\n        const pointLabel = `${label}.points[${pointIndex}]`;\n        const candidate = record(point, pointLabel);\n        exact(candidate, ["date", "balanceMinor", "reserveMinor", "status"], pointLabel);\n        dateTime(candidate.date, `${pointLabel}.date`);\n        safeInteger(candidate.balanceMinor, `${pointLabel}.balanceMinor`);\n        nonNegativeInteger(candidate.reserveMinor, `${pointLabel}.reserveMinor`);\n        enumValue(candidate.status, new Set(["above_reserve", "below_reserve"]), `${pointLabel}.status`);\n      });\n      stringArray(block.assumptions, `${label}.assumptions`, 100);\n      evidenceIds(block.evidenceIds, `${label}.evidenceIds`);\n      break;\n    }\n    case "scenario_compare":\n      exact(block, ["blockId", "type", "baseline", "alternative", "assumptions", "evidenceIds"], label);\n      scenario(block.baseline, `${label}.baseline`);\n      scenario(block.alternative, `${label}.alternative`);\n      stringArray(block.assumptions, `${label}.assumptions`, 100);\n      evidenceIds(block.evidenceIds, `${label}.evidenceIds`);\n      break;\n    case "transaction_rows": {\n      exact(block, ["blockId", "type", "rows", "totalMinor", "currency"], label);\n      const rows = array(block.rows, `${label}.rows`, 1_000);\n      rows.forEach((row, rowIndex) => transaction(row, `${label}.rows[${rowIndex}]`));\n      safeInteger(block.totalMinor, `${label}.totalMinor`);\n      currency(block.currency, `${label}.currency`);\n      break;\n    }\n    case "finding":\n      exact(block, [\n        "blockId", "type", "findingId", "severity", "title", "summary",\n        "amountMinor", "currency", "status", "evidenceIds",\n      ], label);\n      identifier(block.findingId, `${label}.findingId`);\n      enumValue(block.severity, new Set(["info", "attention", "critical"]), `${label}.severity`);\n      requiredString(block.title, `${label}.title`, 240);\n      requiredString(block.summary, `${label}.summary`, 2_000);\n      if (block.amountMinor !== null) safeInteger(block.amountMinor, `${label}.amountMinor`);\n      if (block.currency !== null) currency(block.currency, `${label}.currency`);\n      enumValue(block.status, new Set(["open", "resolved", "dismissed"]), `${label}.status`);\n      evidenceIds(block.evidenceIds, `${label}.evidenceIds`);\n      break;\n    case "source_list": {\n      exact(block, ["blockId", "type", "sources"], label);\n      const sources = array(block.sources, `${label}.sources`, 500);\n      sources.forEach((source, sourceIndex) => {\n        const sourceLabel = `${label}.sources[${sourceIndex}]`;\n        const candidate = record(source, sourceLabel);\n        exact(candidate, ["sourceItemId", "label", "sourceType", "receivedAt", "status"], sourceLabel);\n        identifier(candidate.sourceItemId, `${sourceLabel}.sourceItemId`);\n        requiredString(candidate.label, `${sourceLabel}.label`, 240);\n        enumValue(\n          candidate.sourceType,\n          new Set(["csv", "telegram_fixture", "owner_claim", "akahu_fixture", "plaid_fixture"]),\n          `${sourceLabel}.sourceType`,\n        );\n        dateTime(candidate.receivedAt, `${sourceLabel}.receivedAt`);\n        enumValue(candidate.status, new Set(["pending", "processed", "failed"]), `${sourceLabel}.status`);\n      });\n      break;\n    }\n    case "change_diff": {\n      exact(block, ["blockId", "type", "eventId", "changes", "evidenceIds", "undoAvailable"], label);\n      identifier(block.eventId, `${label}.eventId`);\n      const changes = array(block.changes, `${label}.changes`, 100, 1);\n      changes.forEach((change, changeIndex) => {\n        const changeLabel = `${label}.changes[${changeIndex}]`;\n        const candidate = record(change, changeLabel);\n        exact(candidate, ["field", "label", "before", "after"], changeLabel);\n        enumValue(\n          candidate.field,\n          new Set([\n            "classification", "category", "ruleId", "unresolvedExpenseMinor",\n            "projectedLowPointMinor", "reserveShortfallMinor",\n          ]),\n          `${changeLabel}.field`,\n        );\n        requiredString(candidate.label, `${changeLabel}.label`, 160);\n        scalar(candidate.before, `${changeLabel}.before`);\n        scalar(candidate.after, `${changeLabel}.after`);\n      });\n      evidenceIds(block.evidenceIds, `${label}.evidenceIds`);\n      booleanValue(block.undoAvailable, `${label}.undoAvailable`);\n      break;\n    }\n    case "artifact_preview":\n      exact(block, [\n        "blockId", "type", "artifactId", "kind", "title", "generatedAt",\n        "contentHash", "downloadAvailable", "evidenceIds",\n      ], label);\n      identifier(block.artifactId, `${label}.artifactId`);\n      enumValue(block.kind, new Set(["html", "pdf"]), `${label}.kind`);\n      requiredString(block.title, `${label}.title`, 240);\n      dateTime(block.generatedAt, `${label}.generatedAt`);\n      hash(block.contentHash, `${label}.contentHash`);\n      booleanValue(block.downloadAvailable, `${label}.downloadAvailable`);\n      evidenceIds(block.evidenceIds, `${label}.evidenceIds`);\n      break;\n  }\n  return block as unknown as FinanceBlock;\n}\n\nfunction validateAction(value: unknown, index: number): FinanceAction {\n  const label = `surface.actions[${index}]`;\n  const action = record(value, label);\n  identifier(action.actionId, `${label}.actionId`);\n  const type = enumValue(action.type, actionTypes, `${label}.type`);\n  const keys: Record<string, readonly string[]> = {\n    focus_source: ["actionId", "type", "label", "sourceItemId"],\n    open_drawer: ["actionId", "type", "label", "drawer"],\n    run_scenario: ["actionId", "type", "label", "scenarioId"],\n    undo_event: ["actionId", "type", "label", "eventId"],\n    download_artifact: ["actionId", "type", "label", "artifactId", "format"],\n  };\n  exact(action, keys[type]!, label);\n  requiredString(action.label, `${label}.label`, 160);\n  if (type === "focus_source") identifier(action.sourceItemId, `${label}.sourceItemId`);\n  if (type === "open_drawer") enumValue(action.drawer, new Set(["sources", "activity", "connections"]), `${label}.drawer`);\n  if (type === "run_scenario") identifier(action.scenarioId, `${label}.scenarioId`);\n  if (type === "undo_event") identifier(action.eventId, `${label}.eventId`);\n  if (type === "download_artifact") {\n    identifier(action.artifactId, `${label}.artifactId`);\n    enumValue(action.format, new Set(["html", "pdf"]), `${label}.format`);\n  }\n  return action as unknown as FinanceAction;\n}\n\nexport function validateSurfaceSpec(value: unknown): FinanceSurfaceSpec {\n  const surface = record(value, "finance surface");\n  exact(surface, [\n    "specVersion", "surfaceId", "surfaceType", "title", "subtitle",\n    "freshness", "blocks", "actions",\n  ], "finance surface");\n  if (surface.specVersion !== "FinanceSurfaceSpec@1") throw new Error("Unsupported finance surface version.");\n  identifier(surface.surfaceId, "finance surface.surfaceId");\n  enumValue(surface.surfaceType, surfaceTypes, "finance surface.surfaceType");\n  requiredString(surface.title, "finance surface.title", 240);\n  if (surface.subtitle !== null) requiredString(surface.subtitle, "finance surface.subtitle", 500);\n  freshness(surface.freshness, "finance surface.freshness");\n  const blocks = array(surface.blocks, "finance surface.blocks", 20, 1).map(validateBlock);\n  const actions = array(surface.actions, "finance surface.actions", 8).map(validateAction);\n  unique(blocks.map((block) => block.blockId), "finance surface blocks");\n  unique(actions.map((action) => action.actionId), "finance surface actions");\n  return surface as unknown as FinanceSurfaceSpec;\n}\n\nfunction validateThreadTurn(value: unknown, index: number): ThreadTurn {\n  const label = `snapshot.thread.turns[${index}]`;\n  const turn = record(value, label);\n  exact(\n    turn,\n    ["turnId", "role", "content", "occurredAt", "status", "evidenceIds", "receipt"],\n    label,\n    ["turnId", "role", "content", "occurredAt", "status", "evidenceIds"],\n  );\n  identifier(turn.turnId, `${label}.turnId`);\n  enumValue(turn.role, new Set(["agent", "owner"]), `${label}.role`);\n  requiredString(turn.content, `${label}.content`, 64_000);\n  dateTime(turn.occurredAt, `${label}.occurredAt`);\n  enumValue(turn.status, new Set(["complete", "streaming", "stopped", "failed"]), `${label}.status`);\n  evidenceIds(turn.evidenceIds, `${label}.evidenceIds`);\n  if (turn.receipt !== undefined) {\n    const receipt = record(turn.receipt, `${label}.receipt`);\n    exact(\n      receipt,\n      ["label", "eventId", "undoable"],\n      `${label}.receipt`,\n      ["label"],\n    );\n    requiredString(receipt.label, `${label}.receipt.label`, 240);\n    if (receipt.eventId !== undefined) identifier(receipt.eventId, `${label}.receipt.eventId`);\n    if (receipt.undoable !== undefined) booleanValue(receipt.undoable, `${label}.receipt.undoable`);\n  }\n  return turn as unknown as ThreadTurn;\n}\n\nfunction validateFinding(value: unknown, index: number): void {\n  const label = `snapshot.findings[${index}]`;\n  const finding = record(value, label);\n  exact(finding, [\n    "findingId", "kind", "severity", "title", "summary", "amountMinor",\n    "currency", "status", "evidenceIds",\n  ], label);\n  identifier(finding.findingId, `${label}.findingId`);\n  requiredString(finding.kind, `${label}.kind`, 120);\n  enumValue(finding.severity, new Set(["info", "attention", "critical"]), `${label}.severity`);\n  requiredString(finding.title, `${label}.title`, 240);\n  requiredString(finding.summary, `${label}.summary`, 2_000);\n  if (finding.amountMinor !== null) safeInteger(finding.amountMinor, `${label}.amountMinor`);\n  if (finding.currency !== null) currency(finding.currency, `${label}.currency`);\n  enumValue(finding.status, new Set(["open", "resolved", "dismissed"]), `${label}.status`);\n  evidenceIds(finding.evidenceIds, `${label}.evidenceIds`);\n}\n\nfunction validateActivity(value: unknown, index: number): ActivityItem {\n  const label = `snapshot.activity[${index}]`;\n  const item = record(value, label);\n  exact(\n    item,\n    [\n      "activityId", "kind", "summary", "detail", "status", "occurredAt",\n      "undoable", "correlationId", "eventId", "evidenceIds",\n    ],\n    label,\n    ["activityId", "kind", "summary", "status", "occurredAt", "undoable", "evidenceIds"],\n  );\n  identifier(item.activityId, `${label}.activityId`);\n  enumValue(\n    item.kind,\n    new Set(["job_run", "finance_event", "undo", "source_ingest", "artifact", "outbox"]),\n    `${label}.kind`,\n  );\n  requiredString(item.summary, `${label}.summary`, 500);\n  optionalString(item.detail, `${label}.detail`, 2_000);\n  enumValue(item.status, new Set(["queued", "running", "completed", "undone", "failed"]), `${label}.status`);\n  dateTime(item.occurredAt, `${label}.occurredAt`);\n  booleanValue(item.undoable, `${label}.undoable`);\n  if (item.correlationId !== undefined) identifier(item.correlationId, `${label}.correlationId`);\n  if (item.eventId !== undefined) identifier(item.eventId, `${label}.eventId`);\n  evidenceIds(item.evidenceIds, `${label}.evidenceIds`);\n  return item as unknown as ActivityItem;\n}\n\nfunction validateSource(value: unknown, index: number): SourceItem {\n  const label = `snapshot.sources[${index}]`;\n  const source = record(value, label);\n  exact(\n    source,\n    ["sourceItemId", "sourceType", "label", "receivedAt", "status", "rowCount", "digest"],\n    label,\n    ["sourceItemId", "sourceType", "label", "receivedAt", "status", "rowCount"],\n  );\n  identifier(source.sourceItemId, `${label}.sourceItemId`);\n  enumValue(\n    source.sourceType,\n    new Set(["csv", "telegram_fixture", "owner_claim", "akahu_fixture", "plaid_fixture"]),\n    `${label}.sourceType`,\n  );\n  requiredString(source.label, `${label}.label`, 240);\n  dateTime(source.receivedAt, `${label}.receivedAt`);\n  enumValue(source.status, new Set(["pending", "processed", "failed"]), `${label}.status`);\n  nonNegativeInteger(source.rowCount, `${label}.rowCount`);\n  if (source.digest !== undefined) hash(source.digest, `${label}.digest`);\n  return source as unknown as SourceItem;\n}\n\nexport function validateWorkspaceSnapshot(value: unknown): WorkspaceSnapshot {\n  const snapshot = record(value, "workspace snapshot");\n  exact(snapshot, [\n    "snapshotVersion", "snapshotId", "workspace", "thread", "currentSurface",\n    "findings", "activity", "sources", "totals", "artifacts", "modelMode",\n    "freshness",\n  ], "workspace snapshot");\n  if (snapshot.snapshotVersion !== "api.snapshot@1") throw new Error("Unsupported workspace snapshot version.");\n  identifier(snapshot.snapshotId, "workspace snapshot.snapshotId");\n\n  const workspace = record(snapshot.workspace, "workspace snapshot.workspace");\n  exact(workspace, [\n    "workspaceId", "name", "entityType", "currency", "timezone",\n    "protectedReserveMinor",\n  ], "workspace snapshot.workspace");\n  identifier(workspace.workspaceId, "workspace snapshot.workspace.workspaceId");\n  requiredString(workspace.name, "workspace snapshot.workspace.name", 240);\n  requiredString(workspace.entityType, "workspace snapshot.workspace.entityType", 120);\n  const workspaceCurrency = currency(workspace.currency, "workspace snapshot.workspace.currency");\n  requiredString(workspace.timezone, "workspace snapshot.workspace.timezone", 80);\n  nonNegativeInteger(workspace.protectedReserveMinor, "workspace snapshot.workspace.protectedReserveMinor");\n\n  const thread = record(snapshot.thread, "workspace snapshot.thread");\n  exact(thread, ["threadId", "turns", "activeQuestion"], "workspace snapshot.thread");\n  identifier(thread.threadId, "workspace snapshot.thread.threadId");\n  const turns = array(thread.turns, "workspace snapshot.thread.turns", 10_000).map(validateThreadTurn);\n  unique(turns.map((turn) => turn.turnId), "workspace snapshot turns");\n  if (thread.activeQuestion !== null) {\n    const question = record(thread.activeQuestion, "workspace snapshot.thread.activeQuestion");\n    exact(\n      question,\n      ["questionId", "prompt", "askedAt", "reason"],\n      "workspace snapshot.thread.activeQuestion",\n      ["questionId", "prompt"],\n    );\n    identifier(question.questionId, "workspace snapshot.thread.activeQuestion.questionId");\n    requiredString(question.prompt, "workspace snapshot.thread.activeQuestion.prompt", 2_000);\n    if (question.askedAt !== undefined) dateTime(question.askedAt, "workspace snapshot.thread.activeQuestion.askedAt");\n    optionalString(question.reason, "workspace snapshot.thread.activeQuestion.reason", 1_000);\n  }\n\n  validateSurfaceSpec(snapshot.currentSurface);\n  const findings = array(snapshot.findings, "workspace snapshot.findings", 2_000);\n  findings.forEach(validateFinding);\n  const activities = array(snapshot.activity, "workspace snapshot.activity", 10_000).map(validateActivity);\n  unique(activities.map((item) => item.activityId), "workspace snapshot activity");\n  const sources = array(snapshot.sources, "workspace snapshot.sources", 2_000).map(validateSource);\n  unique(sources.map((item) => item.sourceItemId), "workspace snapshot sources");\n\n  const totals = record(snapshot.totals, "workspace snapshot.totals");\n  const totalFields = [\n    "currentBalanceMinor", "protectedReserveMinor", "businessIncomeMinor",\n    "businessExpenseMinor", "personalExpenseMinor", "unresolvedExpenseMinor",\n    "projectedLowPointMinor", "reserveShortfallMinor",\n  ] as const;\n  exact(totals, ["asOf", "currency", ...totalFields], "workspace snapshot.totals");\n  dateTime(totals.asOf, "workspace snapshot.totals.asOf");\n  if (currency(totals.currency, "workspace snapshot.totals.currency") !== workspaceCurrency) {\n    throw new Error("workspace snapshot totals currency must match the workspace currency.");\n  }\n  totalFields.forEach((field) => safeInteger(totals[field], `workspace snapshot.totals.${field}`));\n  if ((totals.protectedReserveMinor as number) < 0 || (totals.reserveShortfallMinor as number) < 0) {\n    throw new Error("workspace snapshot reserve totals must not be negative.");\n  }\n\n  const artifacts = array(snapshot.artifacts, "workspace snapshot.artifacts", 2_000);\n  const artifactIds: string[] = [];\n  artifacts.forEach((artifactValue, index) => {\n    const label = `workspace snapshot.artifacts[${index}]`;\n    const artifact = record(artifactValue, label);\n    exact(artifact, [\n      "artifactId", "kind", "title", "contentHash", "generatedAt", "evidenceIds",\n    ], label);\n    artifactIds.push(identifier(artifact.artifactId, `${label}.artifactId`));\n    enumValue(artifact.kind, new Set(["owner_pack_html", "owner_pack_pdf"]), `${label}.kind`);\n    requiredString(artifact.title, `${label}.title`, 240);\n    hash(artifact.contentHash, `${label}.contentHash`);\n    dateTime(artifact.generatedAt, `${label}.generatedAt`);\n    evidenceIds(artifact.evidenceIds, `${label}.evidenceIds`);\n  });\n  unique(artifactIds, "workspace snapshot artifacts");\n  enumValue(snapshot.modelMode, new Set(["local", "hybrid", "cloud"]), "workspace snapshot.modelMode");\n  freshness(snapshot.freshness, "workspace snapshot.freshness");\n  return snapshot as unknown as WorkspaceSnapshot;\n}\n\nfunction validateRunPayload(type: string, value: unknown): void {\n  const label = `${type} payload`;\n  const payload = record(value, label);\n  const keys: Record<string, readonly string[]> = {\n    "run.started": ["mode", "reason", "resumeFromSequence"],\n    "message.delta": ["turnId", "delta"],\n    "message.completed": ["turnId", "content", "evidenceIds"],\n    "stage.started": ["stage"],\n    "stage.completed": ["stage", "status", "durationMs"],\n    "tool.started": ["toolCallId", "toolName"],\n    "tool.completed": ["toolCallId", "toolName", "status", "durationMs", "evidenceIds"],\n    "state.snapshot": ["snapshot"],\n    "state.patch": ["baseSnapshotId", "snapshot"],\n    "surface.replace": ["surface"],\n    "surface.patch": ["surfaceId", "surface"],\n    "receipt.committed": ["receiptType", "receiptId", "contentHash", "evidenceIds"],\n    "run.failed": ["code", "message", "retryable", "lastSequence"],\n    "run.completed": ["status", "durationMs", "snapshotId", "receiptId"],\n  };\n  exact(payload, keys[type]!, label);\n  switch (type) {\n    case "run.started":\n      enumValue(payload.mode, new Set(["local", "hybrid", "cloud"]), `${label}.mode`);\n      requiredString(payload.reason, `${label}.reason`, 120);\n      if (payload.resumeFromSequence !== null) nonNegativeInteger(payload.resumeFromSequence, `${label}.resumeFromSequence`);\n      break;\n    case "message.delta":\n      identifier(payload.turnId, `${label}.turnId`);\n      requiredString(payload.delta, `${label}.delta`, 8_000);\n      break;\n    case "message.completed":\n      identifier(payload.turnId, `${label}.turnId`);\n      requiredString(payload.content, `${label}.content`, 64_000);\n      evidenceIds(payload.evidenceIds, `${label}.evidenceIds`);\n      break;\n    case "stage.started":\n      requiredString(payload.stage, `${label}.stage`, 120);\n      break;\n    case "stage.completed":\n      requiredString(payload.stage, `${label}.stage`, 120);\n      requiredString(payload.status, `${label}.status`, 80);\n      nonNegativeInteger(payload.durationMs, `${label}.durationMs`);\n      break;\n    case "tool.started":\n      identifier(payload.toolCallId, `${label}.toolCallId`);\n      requiredString(payload.toolName, `${label}.toolName`, 120);\n      break;\n    case "tool.completed":\n      identifier(payload.toolCallId, `${label}.toolCallId`);\n      requiredString(payload.toolName, `${label}.toolName`, 120);\n      requiredString(payload.status, `${label}.status`, 80);\n      nonNegativeInteger(payload.durationMs, `${label}.durationMs`);\n      evidenceIds(payload.evidenceIds, `${label}.evidenceIds`);\n      break;\n    case "state.snapshot":\n      validateWorkspaceSnapshot(payload.snapshot);\n      break;\n    case "state.patch":\n      identifier(payload.baseSnapshotId, `${label}.baseSnapshotId`);\n      validateWorkspaceSnapshot(payload.snapshot);\n      break;\n    case "surface.replace":\n      validateSurfaceSpec(payload.surface);\n      break;\n    case "surface.patch":\n      identifier(payload.surfaceId, `${label}.surfaceId`);\n      validateSurfaceSpec(payload.surface);\n      break;\n    case "receipt.committed":\n      requiredString(payload.receiptType, `${label}.receiptType`, 120);\n      identifier(payload.receiptId, `${label}.receiptId`);\n      hash(payload.contentHash, `${label}.contentHash`);\n      evidenceIds(payload.evidenceIds, `${label}.evidenceIds`);\n      break;\n    case "run.failed":\n      requiredString(payload.code, `${label}.code`, 120);\n      requiredString(payload.message, `${label}.message`, 2_000);\n      booleanValue(payload.retryable, `${label}.retryable`);\n      nonNegativeInteger(payload.lastSequence, `${label}.lastSequence`);\n      break;\n    case "run.completed":\n      requiredString(payload.status, `${label}.status`, 80);\n      nonNegativeInteger(payload.durationMs, `${label}.durationMs`);\n      identifier(payload.snapshotId, `${label}.snapshotId`);\n      identifier(payload.receiptId, `${label}.receiptId`);\n      break;\n  }\n}\n\nexport function validateRunEvent(value: unknown): RunEvent {\n  const event = record(value, "run event");\n  exact(event, ["eventId", "threadId", "runId", "sequence", "occurredAt", "type", "payload"], "run event");\n  identifier(event.eventId, "run event.eventId");\n  identifier(event.threadId, "run event.threadId");\n  identifier(event.runId, "run event.runId");\n  const sequence = safeInteger(event.sequence, "run event.sequence");\n  if (sequence < 1) throw new Error("run event.sequence must be positive.");\n  dateTime(event.occurredAt, "run event.occurredAt");\n  const type = enumValue(event.type, runEventTypes, "run event.type");\n  validateRunPayload(type, event.payload);\n  return event as unknown as RunEvent;\n}\n\nexport const runtimeContractInternals = { finiteNumber };\n''',
    )


def create_http_errors() -> None:
    write(
        "apps/desktop/src/httpErrors.ts",
        '''type ProblemDetails = {\n  type?: unknown;\n  title?: unknown;\n  detail?: unknown;\n  requestId?: unknown;\n};\n\nfunction boundedText(value: unknown, fallback: string, maximum = 600): string {\n  if (typeof value !== "string") return fallback;\n  const normalised = value.replace(/[\\r\\n\\t]+/g, " ").trim();\n  return normalised ? normalised.slice(0, maximum) : fallback;\n}\n\nexport class FolioHttpError extends Error {\n  readonly status: number;\n  readonly problemType: string;\n  readonly detail: string;\n  readonly requestId?: string;\n\n  constructor(options: {\n    status: number;\n    problemType: string;\n    title: string;\n    detail: string;\n    requestId?: string;\n  }) {\n    super(`${options.status} ${options.title}: ${options.detail}`);\n    this.name = "FolioHttpError";\n    this.status = options.status;\n    this.problemType = options.problemType;\n    this.detail = options.detail;\n    this.requestId = options.requestId;\n  }\n}\n\nexport async function throwForResponse(response: Response): Promise<never> {\n  const fallbackTitle = boundedText(response.statusText, "Request failed", 120);\n  const headerRequestId = boundedText(response.headers.get("x-request-id"), "", 120) || undefined;\n  let problem: ProblemDetails = {};\n  const contentType = response.headers.get("content-type")?.toLowerCase() ?? "";\n  if (contentType.includes("application/problem+json") || contentType.includes("application/json")) {\n    try {\n      const candidate = await response.clone().json();\n      if (candidate && typeof candidate === "object" && !Array.isArray(candidate)) {\n        problem = candidate as ProblemDetails;\n      }\n    } catch {\n      // Fall through to bounded protocol metadata. Do not expose raw response bodies.\n    }\n  }\n  const title = boundedText(problem.title, fallbackTitle, 160);\n  const detail = boundedText(problem.detail, "The local Folio request did not complete.", 600);\n  const problemType = boundedText(problem.type, "about:blank", 240);\n  const requestId = boundedText(problem.requestId, headerRequestId ?? "", 120) || undefined;\n  throw new FolioHttpError({\n    status: response.status, problemType, title, detail, requestId,\n  });\n}\n''',
    )


def patch_types() -> None:
    path = "apps/desktop/src/types.ts"
    value = read(path)
    value = replace_once(
        value,
        '    activeQuestion: null | { questionId: string; prompt: string };\n',
        '    activeQuestion: null | { questionId: string; prompt: string; askedAt?: string; reason?: string };\n',
        label="active question runtime fields",
    )
    value = replace_once(
        value,
        '  status: "complete" | "streaming" | "stopped";\n',
        '  status: "complete" | "streaming" | "stopped" | "failed";\n',
        label="failed turn status",
    )
    write(path, value)


def patch_transport() -> None:
    path = "apps/desktop/src/transport.ts"
    value = read(path)
    first_line = 'import { validateRunEvent, validateSurfaceSpec, type RunEvent, type WorkspaceSnapshot } from "./types";\n'
    replacement = (
        'import { throwForResponse } from "./httpErrors";\n'
        'import { validateRunEvent, validateWorkspaceSnapshot } from "./runtimeContracts";\n'
        'import type { RunEvent, WorkspaceSnapshot } from "./types";\n'
    )
    value = replace_once(value, first_line, replacement, label="transport contract imports")
    value = replace_once(
        value,
        '      throw new Error(`${response.status} ${response.statusText}`);\n',
        '      await throwForResponse(response);\n',
        label="requestJson problem details",
    )
    value = replace_once(
        value,
        '''export async function loadSnapshot(workspaceId: string): Promise<WorkspaceSnapshot> {\n  const snapshot = await requestJson<WorkspaceSnapshot>(`/v1/workspaces/${workspaceId}/snapshot`, undefined, 2800);\n  return {\n    ...snapshot,\n    currentSurface: validateSurfaceSpec(snapshot.currentSurface),\n  };\n}\n''',
        '''export async function loadSnapshot(workspaceId: string): Promise<WorkspaceSnapshot> {\n  const snapshot = await requestJson<unknown>(`/v1/workspaces/${workspaceId}/snapshot`, undefined, 2800);\n  return validateWorkspaceSnapshot(snapshot);\n}\n''',
        label="complete snapshot validation",
    )
    value = value.replace(
        'if (!response.ok) throw new Error(`${response.status} ${response.statusText}`);',
        'if (!response.ok) await throwForResponse(response);',
    )
    write(path, value)


def patch_surface_renderer() -> None:
    path = "apps/desktop/src/SurfaceRenderer.tsx"
    value = read(path)
    value = replace_once(
        value,
        'import {\n  validateSurfaceSpec,\n  type CashSeriesBlock,\n',
        'import { validateSurfaceSpec } from "./runtimeContracts";\nimport {\n  type CashSeriesBlock,\n',
        label="surface runtime validator import",
    )
    risk_anchor = '''      <div className="chart-risk-note">\n        {firstBreach ? <WarningIcon size={15} /> : <CheckIcon size={15} />}\n        {riskSummary}\n      </div>\n'''
    table = '''      <table className="visually-hidden">\n        <caption>Thirty-day projected cash balance and protected reserve data</caption>\n        <thead>\n          <tr><th scope="col">Date</th><th scope="col">Projected cash</th><th scope="col">Protected reserve</th><th scope="col">Status</th></tr>\n        </thead>\n        <tbody>\n          {block.points.map((point) => (\n            <tr key={`cash-data-${point.date}`}>\n              <td>{formatDate(point.date)}</td>\n              <td>{formatMoney(point.balanceMinor, block.currency)}</td>\n              <td>{formatMoney(point.reserveMinor, block.currency)}</td>\n              <td>{point.status === "below_reserve" ? "Below reserve" : "At or above reserve"}</td>\n            </tr>\n          ))}\n        </tbody>\n      </table>\n'''
    value = replace_once(value, risk_anchor, table + risk_anchor, label="cash chart data table")
    write(path, value)


def patch_styles() -> None:
    path = "apps/desktop/src/styles.css"
    value = read(path)
    if ".visually-hidden" not in value:
        value = value.rstrip() + '''\n\n.visually-hidden {\n  position: absolute !important;\n  width: 1px !important;\n  height: 1px !important;\n  padding: 0 !important;\n  margin: -1px !important;\n  overflow: hidden !important;\n  clip: rect(0, 0, 0, 0) !important;\n  white-space: nowrap !important;\n  border: 0 !important;\n}\n'''
    write(path, value)


def create_tests() -> None:
    write(
        "apps/desktop/src/runtimeContracts.test.ts",
        '''import assert from "node:assert/strict";\nimport test from "node:test";\n\nimport { validateRunEvent, validateSurfaceSpec, validateWorkspaceSnapshot } from "./runtimeContracts.js";\n\nfunction validSurface() {\n  return {\n    specVersion: "FinanceSurfaceSpec@1",\n    surfaceId: "surface_test_current",\n    surfaceType: "living_brief",\n    title: "Current picture",\n    subtitle: null,\n    freshness: { dataThrough: "2026-08-27T00:00:00+12:00", status: "current", timezone: "Pacific/Auckland" },\n    blocks: [\n      {\n        blockId: "block_test_metric", type: "metric", label: "Balance",\n        valueMinor: 10000, currency: "NZD", evidenceIds: ["evd_test_balance"],\n      },\n    ],\n    actions: [],\n  };\n}\n\nfunction validSnapshot() {\n  return {\n    snapshotVersion: "api.snapshot@1",\n    snapshotId: "snap_test_current",\n    workspace: {\n      workspaceId: "ws_test_business", name: "Test Business", entityType: "nz_sole_trader",\n      currency: "NZD", timezone: "Pacific/Auckland", protectedReserveMinor: 5000,\n    },\n    thread: {\n      threadId: "thr_test_main",\n      turns: [\n        {\n          turnId: "turn_test_owner", role: "owner", content: "What changed?",\n          occurredAt: "2026-08-27T00:01:00+12:00", status: "complete", evidenceIds: [],\n        },\n      ],\n      activeQuestion: null,\n    },\n    currentSurface: validSurface(),\n    findings: [], activity: [], sources: [],\n    totals: {\n      asOf: "2026-08-27T00:00:00+12:00", currency: "NZD",\n      currentBalanceMinor: 10000, protectedReserveMinor: 5000,\n      businessIncomeMinor: 10000, businessExpenseMinor: 0, personalExpenseMinor: 0,\n      unresolvedExpenseMinor: 0, projectedLowPointMinor: 10000, reserveShortfallMinor: 0,\n    },\n    artifacts: [], modelMode: "local",\n    freshness: { dataThrough: "2026-08-27T00:00:00+12:00", status: "current", timezone: "Pacific/Auckland" },\n  };\n}\n\ntest("accepts a complete valid snapshot and surface", () => {\n  const snapshot = validSnapshot();\n  assert.equal(validateWorkspaceSnapshot(snapshot), snapshot);\n  assert.equal(validateSurfaceSpec(snapshot.currentSurface), snapshot.currentSurface);\n});\n\ntest("rejects unknown top-level and nested contract fields", () => {\n  assert.throws(\n    () => validateWorkspaceSnapshot({ ...validSnapshot(), executableCode: "alert(1)" }),\n    /outside the closed contract/,\n  );\n  const surface = validSurface();\n  surface.blocks[0] = { ...surface.blocks[0], arbitrary: true } as never;\n  assert.throws(() => validateSurfaceSpec(surface), /outside the closed contract/);\n});\n\ntest("rejects unsafe integers, invalid enums and duplicate identifiers", () => {\n  const unsafe = validSnapshot();\n  unsafe.totals.currentBalanceMinor = Number.MAX_SAFE_INTEGER + 1;\n  assert.throws(() => validateWorkspaceSnapshot(unsafe), /safe integer/);\n\n  const invalidStatus = validSnapshot();\n  invalidStatus.freshness.status = "guessed" as never;\n  assert.throws(() => validateWorkspaceSnapshot(invalidStatus), /outside the closed contract/);\n\n  const duplicate = validSurface();\n  duplicate.blocks.push({ ...duplicate.blocks[0]! });\n  assert.throws(() => validateSurfaceSpec(duplicate), /duplicate identifiers/);\n});\n\ntest("validates embedded snapshot state in ordered run events", () => {\n  const snapshot = validSnapshot();\n  const event = {\n    eventId: "streamevt_test_state", threadId: "thr_test_main", runId: "run_test_state",\n    sequence: 1, occurredAt: "2026-08-27T00:02:00+12:00", type: "state.snapshot",\n    payload: { snapshot },\n  };\n  assert.equal(validateRunEvent(event), event);\n  assert.throws(() => validateRunEvent({ ...event, sequence: 0 }), /positive/);\n  const invalid = structuredClone(event);\n  invalid.payload.snapshot.totals.currency = "USD";\n  assert.throws(() => validateRunEvent(invalid), /must match/);\n});\n''',
    )
    write(
        "apps/desktop/src/httpErrors.test.ts",
        '''import assert from "node:assert/strict";\nimport test from "node:test";\n\nimport { FolioHttpError, throwForResponse } from "./httpErrors.js";\n\ntest("preserves bounded problem details without exposing raw response data", async () => {\n  const response = new Response(JSON.stringify({\n    type: "https://folio.local/problems/invalid-import",\n    title: "Statement rejected",\n    detail: "The statement headings are ambiguous.",\n    requestId: "request_test_001",\n    secret: "must-not-escape",\n  }), {\n    status: 422,\n    headers: { "Content-Type": "application/problem+json" },\n  });\n\n  await assert.rejects(\n    () => throwForResponse(response),\n    (error: unknown) => {\n      assert.ok(error instanceof FolioHttpError);\n      assert.equal(error.status, 422);\n      assert.equal(error.problemType, "https://folio.local/problems/invalid-import");\n      assert.equal(error.detail, "The statement headings are ambiguous.");\n      assert.equal(error.requestId, "request_test_001");\n      assert.equal(error.message.includes("must-not-escape"), false);\n      return true;\n    },\n  );\n});\n\ntest("uses protocol metadata when an error body is malformed", async () => {\n  const response = new Response("private upstream body", {\n    status: 502,\n    statusText: "Bad Gateway",\n    headers: { "Content-Type": "text/plain", "X-Request-Id": "request_gateway_1" },\n  });\n  await assert.rejects(\n    () => throwForResponse(response),\n    (error: unknown) => {\n      assert.ok(error instanceof FolioHttpError);\n      assert.equal(error.status, 502);\n      assert.equal(error.detail, "The local Folio request did not complete.");\n      assert.equal(error.requestId, "request_gateway_1");\n      assert.equal(error.message.includes("private upstream body"), false);\n      return true;\n    },\n  );\n});\n''',
    )
    write(
        "apps/desktop/tsconfig.tests.json",
        '''{\n  "extends": "../../tsconfig.base.json",\n  "compilerOptions": {\n    "target": "ES2022",\n    "lib": ["ES2022", "DOM", "DOM.Iterable"],\n    "module": "NodeNext",\n    "moduleResolution": "NodeNext",\n    "types": ["node"],\n    "outDir": "dist-tests",\n    "rootDir": "src",\n    "noEmit": false,\n    "allowImportingTsExtensions": false\n  },\n  "include": [\n    "src/types.ts",\n    "src/runtimeContracts.ts",\n    "src/httpErrors.ts",\n    "src/runtimeContracts.test.ts",\n    "src/httpErrors.test.ts"\n  ]\n}\n''',
    )


def patch_scripts() -> None:
    path = "apps/desktop/package.json"
    value = json.loads(read(path))
    value["scripts"]["test:renderer-contracts"] = (
        "node -e \"require('node:fs').rmSync('dist-tests',{recursive:true,force:true})\" "
        "&& tsc -p tsconfig.tests.json "
        "&& node --test dist-tests/runtimeContracts.test.js dist-tests/httpErrors.test.js"
    )
    existing = value["scripts"].get("test", "pnpm build:electron && node --test dist-electron/main/security.test.js")
    if "test:renderer-contracts" not in existing:
        value["scripts"]["test"] = existing + " && pnpm test:renderer-contracts"
    write(path, json.dumps(value, indent=2) + "\n")


def main() -> None:
    create_runtime_contracts()
    create_http_errors()
    patch_types()
    patch_transport()
    patch_surface_renderer()
    patch_styles()
    create_tests()
    patch_scripts()


if __name__ == "__main__":
    main()
